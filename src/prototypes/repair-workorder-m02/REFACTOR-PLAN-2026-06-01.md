# 维修工单管理 - 信息重组实施计划

**创建日期**：2026-06-01  
**优先级**：高  
**预计工作量**：大型重构（8-10小时）

---

## 📋 需求概述

重新组织创建工单页面的信息架构，将信息分为 7 大类，新增大量字段，同时保持原有逻辑不被破坏。

---

## 🎯 信息分类需求

### 1. 车主信息
**新增字段**：
- ✅ 车主（已有：owner）
- ❌ 车主类别（个人/企业/事业）**【新增】**
- ❌ 联系人 **【新增】**
- ❌ 联系方式（电话/Email）**【新增】**
- ✅ 送修人（已有：sender）
- ✅ 送修人联系方式（已有：senderPhone）
- ❌ 送修人 Email **【新增】**

### 2. 车辆信息
**新增字段**：
- ❌ 品牌 **【新增】**
- ✅ 车系（已有：series）
- ✅ 车型（已有：model）
- ❌ 配置 **【新增】**
- ✅ 外饰（已有：color）
- ❌ 内饰 **【新增】**
- ✅ 进厂里程（已有：mileage）
- ❌ 累计里程 **【新增】**
- ❌ 换表里程 **【新增】**
- ❌ 下次保养日期 **【新增】**
- ✅ 销售日期（已有：saleDate）
- ❌ 保修到期日期 **【新增】**
- ❌ 保险到期日期 **【新增】**
- ❌ 是否三包 **【新增】**

### 3. 维修项目信息
**字段调整**：
- ✅ 项目代码（已有：code）
- ✅ 项目名称（已有：name）
- ❌ 套餐/活动名称 **【新增】**
- ✅ 工时单价（已有：unitPrice）
- ✅ 标准工时（已有：hours）
- ✅ 总价格（已有：fee）
- ✅ 收费类型（已有：chargeType）
- ✅ 折扣（已有：discountRate）
- ❌ 故障部位 **【新增】**
- ❌ 技师 **【派工后回写】**
- ❌ 工位 **【派工后回写】**

### 4. 备件信息
**字段调整**：
- ✅ 备件代码（已有：code）
- ✅ 备件名称（已有：name）
- ❌ 套餐/活动 **【新增】**
- ✅ 单价（已有：unitPrice）
- ✅ 数量（已有：qty）
- ✅ 总价格（已有：fee）
- ✅ 收费区分（已有：chargeType）
- ✅ 折扣（已有：discountRate）
- ❌ 发料人 **【领料后回写】**
- ❌ 领料人 **【领料后回写】**

### 5. 其他费用
**新增模块**：
- ❌ 关联单据号（取送车、道路救援）**【全新模块】**
- ✅ 费用名称（已有：其他费用区域）
- ✅ 金额（已有）
- ✅ 收费类型（已有）
- ✅ 备注（已有）

### 6. 费用总计
**字段调整**：
- ✅ 工时费（已有：actualLaborFee）
- ✅ 备件费（已有：actualMaterialFee）
- ❌ 附件费 **【新增】** → 应该是"其他费用"
- ✅ 折扣（已有：在项目和备件中）
- ✅ 预估总额（已有：actualTotalFee）

### 7. 车主标签 & 车辆标签
**现状**：
- ✅ 车主标签已部分实现（相关标签摘要）
- ❌ 需要新增标签字段（健身达人、敏感客户等）**【新增】**
- ✅ 车辆标签已实现（按时保养、降级车等）

### 8. 工单信息
**新增字段**：
- ✅ 工单号（已有：id）
- ✅ 工单类型（已有：repairType）
- ✅ 服务顾问（已有：advisor）
- ✅ 开单时间（已有：createdAt）
- ✅ 预计交车时间（已有：estimatedDeliveryAt）
- ✅ 保险公司（已有：insuranceCompany）
- ❌ 是否送修 **【新增】**
- ❌ 责任技师 **【新增】**

---

## 📊 数据模型扩展

### WorkOrder 接口新增字段

```typescript
interface WorkOrder {
  // ... 现有字段
  
  // 车主信息扩展
  ownerType?: '个人' | '企业' | '事业' // 车主类别
  contactPerson?: string // 联系人
  contactPhone?: string // 联系人电话
  contactEmail?: string // 联系人邮箱
  ownerEmail?: string // 车主邮箱
  senderEmail?: string // 送修人邮箱
  
  // 车辆信息扩展
  brand?: string // 品牌
  configuration?: string // 配置
  interiorColor?: string // 内饰
  totalMileage?: number // 累计里程
  replacedMileage?: number // 换表里程
  nextMaintenanceDate?: string // 下次保养日期
  warrantyExpireDate?: string // 保修到期日期
  insuranceExpireDate?: string // 保险到期日期
  isThreePack?: boolean // 是否三包
  
  // 车主标签
  ownerTags?: string[] // 车主标签（健身达人、敏感客户等）
  
  // 工单信息扩展
  isPickupDelivery?: boolean // 是否送修
  principalTechnician?: string // 责任技师
}
```

### RepairItem 接口新增字段

```typescript
interface RepairItem {
  // ... 现有字段
  
  packageName?: string // 套餐/活动名称
  faultLocation?: string // 故障部位
  assignedTechnician?: string // 指派技师（派工后回写）
  workstation?: string // 工位（派工后回写）
}
```

### PartItem 接口新增字段

```typescript
interface PartItem {
  // ... 现有字段
  
  packageName?: string // 套餐/活动
  issuer?: string // 发料人（领料后回写）
  receiver?: string // 领料人（领料后回写）
  issuedAt?: string // 发料时间
}
```

### OtherFeeItem 接口扩展

```typescript
interface OtherFeeItem {
  id: string
  name: string // 费用名称
  amount: number // 金额
  chargeType: string // 收费类型
  note?: string // 备注
  relatedDocNo?: string // 关联单据号（取送车、道路救援）
  relatedDocType?: '取送车' | '道路救援' | '其他' // 关联单据类型
}
```

---

## 🎨 UI 重组方案

### 创建工单页面结构（新）

```
创建工单
├── 顶部车辆概览（保持不变）
├── 1️⃣ 车主信息卡片
│   ├── 车主类别（新增下拉框）
│   ├── 车主姓名
│   ├── 车主电话
│   ├── 车主邮箱（新增）
│   ├── 联系人（新增）
│   ├── 联系人电话（新增）
│   ├── 联系人邮箱（新增）
│   ├── 送修人
│   ├── 送修人电话
│   └── 送修人邮箱（新增）
├── 2️⃣ 车辆信息卡片
│   ├── 品牌（新增）
│   ├── 车系
│   ├── 车型
│   ├── 配置（新增）
│   ├── 外饰颜色
│   ├── 内饰颜色（新增）
│   ├── VIN
│   ├── 发动机号
│   ├── 进厂里程
│   ├── 累计里程（新增）
│   ├── 换表里程（新增）
│   ├── 销售日期
│   ├── 下次保养日期（新增）
│   ├── 保修到期日期（新增）
│   ├── 保险到期日期（新增）
│   ├── 保险公司
│   └── 是否三包（新增checkbox）
├── 3️⃣ 车主标签 & 车辆标签卡片
│   ├── 车主标签（健身达人、敏感客户等）
│   └── 车辆标签（按时保养、降级车等）
├── 4️⃣ 工单信息卡片
│   ├── 工单号（自动生成）
│   ├── 工单类型
│   ├── 服务顾问
│   ├── 开单时间（自动填充）
│   ├── 预计交车时间
│   ├── 是否送修（新增checkbox）
│   └── 责任技师（新增）
├── 5️⃣ 维修项目信息
│   ├── 表格列：项目代码、项目名称、套餐/活动名称（新增）、工时单价、标准工时、总价格、收费类型、折扣、故障部位（新增）
│   └── 派工后显示：技师、工位
├── 6️⃣ 备件信息
│   ├── 表格列：备件代码、备件名称、套餐/活动（新增）、单价、数量、总价格、收费区分、折扣
│   └── 领料后显示：发料人、领料人
├── 7️⃣ 其他费用
│   └── 表格列：费用名称、金额、收费类型、关联单据号（新增）、关联单据类型（新增）、备注
└── 8️⃣ 费用总计
    ├── 工时费
    ├── 备件费
    ├── 其他费用合计（原"附件费"）
    ├── 折扣汇总
    └── 预估总额
```

---

## ⚠️ 重要约束

1. **不破坏现有逻辑**：
   - 保留所有现有字段和功能
   - 现有的派工、领料、质检、交车流程不变
   - 三包预警、整合弹窗等功能保持不变

2. **向后兼容**：
   - 新增字段全部设为可选（`?`）
   - 现有数据不受影响
   - 示例数据保持兼容

3. **逐步回写**：
   - 技师、工位在派工完成后回写
   - 发料人、领料人在领料完成后回写
   - 详情页显示完整信息

---

## 🚀 实施步骤

### Phase 1: 数据模型扩展（1小时）
- [x] 扩展 WorkOrder 接口
- [x] 扩展 RepairItem 接口
- [x] 扩展 PartItem 接口
- [x] 新增 OtherFeeItem 接口
- [x] 更新示例数据

### Phase 2: 创建工单页面重组（4小时）
- [x] 新增"车主信息"卡片
- [x] 重组"车辆信息"卡片
- [x] 新增"车主标签 & 车辆标签"卡片
- [x] 新增"工单信息"卡片
- [x] 调整"维修项目信息"表格
- [x] 调整"备件信息"表格
- [x] 重构"其他费用"模块
- [x] 调整"费用总计"显示

### Phase 3: 派工回写逻辑（1小时）
- [x] 派工时回写技师和工位到维修项目
- [x] 派工详情页显示技师和工位

### Phase 4: 领料回写逻辑（1小时）
- [x] 领料时回写发料人和领料人到备件
- [x] 备件明细显示发料人和领料人

### Phase 5: 详情页更新（1小时）
- [x] 更新委托书详情页信息展示
- [x] 按新的分类展示所有字段

### Phase 6: 测试与验收（1小时）
- [x] 功能测试（详情页卡片/表格/费用汇总浏览器验收）
- [x] 数据兼容性测试（无 additionalFeeItems 时回退 otherFee）
- [x] 交互流程测试（派工/领料/编辑模式逻辑未破坏）

---

## 📝 技术实现要点

1. **字段分组函数**：
   ```typescript
   const getOwnerInfoFields = (order: WorkOrder) => [...]
   const getVehicleInfoFields = (order: WorkOrder) => [...]
   const getOrderInfoFields = (order: WorkOrder) => [...]
   ```

2. **回写逻辑**：
   ```typescript
   const assignTechnicianToRepairItems = (orderId, assignments) => {
     // 派工时回写技师和工位
   }
   
   const recordMaterialIssue = (orderId, partId, issuer, receiver) => {
     // 领料时回写发料人和领料人
   }
   ```

3. **条件显示**：
   - 创建时：不显示技师、工位、发料人、领料人
   - 派工后：显示技师和工位
   - 领料后：显示发料人和领料人

---

## 📊 影响范围评估

### 需要修改的文件
- `src/prototypes/repair-workorder-m02/index.tsx` - 主文件（大量修改）
- `src/prototypes/repair-workorder-m02/spec.md` - 规格文档更新

### 需要更新的功能
- ✅ 创建工单页面（大改）
- ✅ 委托书详情页面（中改）
- ✅ 派工逻辑（小改 - 新增回写）
- ✅ 领料逻辑（小改 - 新增回写）
- ✅ 编辑工单功能（需适配新字段）

### 不受影响的功能
- ✅ 工单列表
- ✅ 三包预警
- ✅ 质检流程
- ✅ 交车流程
- ✅ 上门服务

---

## 💡 建议

由于这是一个**大型重构**，建议：

1. **分阶段实施**：先完成数据模型，再逐个卡片重构
2. **保持版本控制**：每完成一个Phase提交一次
3. **充分测试**：每个Phase完成后测试原有功能是否正常
4. **文档同步**：及时更新 spec.md 和相关文档

---

## ❓ 需要确认的问题

1. **车主类别**：除了"个人/企业/事业"，是否还有其他类型？
2. **车主标签**：除了"健身达人、敏感客户"，还需要哪些标签？
3. **套餐/活动名称**：是否需要从系统中选择，还是手工输入？
4. **关联单据**：取送车和道路救援单据号格式是什么？
5. **工位编号**：工位的编号规则是什么？
6. **责任技师**：是否等同于主技师，还是单独字段？

---

**状态**：等待确认后开始实施  
**预计完成时间**：8-10 小时（分多次完成）
